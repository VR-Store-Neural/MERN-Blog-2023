// up trend
// let V1, V2, V3

// let V = V1 - V2;
// let V0 = V2 - V3;

// if (V1 > V2) {
//     if (V2 > V3) {
//         console.log("Volume UP1");
//     } else (V2 = V3) {
//         console.log("Volume UP2");
//     } else (V2 < V3)
  
    let x, y, z
    
    if (x > y) {
      if (y > z) {
        console.log("x является наибольшим числом");
      } if (y = z) {
            console.log("x является наибольшим числом");
      } if
        console.log("z является наибольшим числом");
      }
    } else {
      if (y > z) {
        console.log("y является наибольшим числом");
      } else {
        console.log("z является наибольшим числом");
      }
    }